# Metadata

Status: ACTIVE
Date: 2026-09-04

Lightweight MVP structured metadata lives here as JSON or CSV. Files in this tree must validate against schemas in `schemas/` when a schema exists.

Drive file and folder names are not the database. Use stable IDs inside records.
